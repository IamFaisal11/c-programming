#include<stdio.h>
int sum();
int main()
{
    int a, b;
 int ans;
    printf("enter the value of x and y\n");
    scanf("%d %d",&a,&b);
    ans=sum(a, b);
    printf("the sum of  %d and %d is %d",a,b,ans);
    
}
int sum(int x, int y)
{ 
    int z;
    z=x+y;
}
